import { ROUNDED_BUTTONS_ICONS } from "src/constants/items"
import { IRoundButtonConfig } from "./round-button.component"


export const ROUND_BUTTON_CREATE_CONFIG: IRoundButtonConfig = {
    iconName: ROUNDED_BUTTONS_ICONS.CREATE,
    extraClass: null,
    lowerButton: false,
    secondaryButton: false
}

export const ROUND_BUTTON_EXCEL_CONFIG: IRoundButtonConfig = {
    iconName: ROUNDED_BUTTONS_ICONS.CREATE_MULTIPLE,
    extraClass: null,
    lowerButton: false,
    secondaryButton: false
}

export const ROUND_BUTTON_DELETE_CONFIG: IRoundButtonConfig = {
    iconName: ROUNDED_BUTTONS_ICONS.DELETE,
    extraClass: null,
    lowerButton: false,
    secondaryButton: false
}

export const ROUND_BUTTON_EDIT_CONFIG: IRoundButtonConfig = {
    iconName: ROUNDED_BUTTONS_ICONS.EDIT,
    extraClass: null,
    lowerButton: false,
    secondaryButton: false
}
